name: voice_ai
description: Voice AI课堂助手
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^0.13.6
  pinyin: ^3.3.0
  shared_preferences: ^2.2.2
  flutter_spinkit: ^5.2.0
  vosk_flutter:
    path: packages/vosk_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
